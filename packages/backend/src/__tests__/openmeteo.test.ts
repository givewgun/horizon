import { describe, expect, it } from 'vitest';

describe('OpenMeteo normalizer', () => {
  it('normalizes Open-Meteo response to Forecast type', () => {
    // Import the normalizer once it's exported
    // For now, we verify the Forecast type shape in components
    expect(true).toBe(true);
  });
});
